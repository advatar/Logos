[package]
name = "zk-membership-guest"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
# risc0-zkvm = { version = "3.0.5", default-features = false, features = ["std"] }
# protocol-core = { path = "../../crates/protocol-core" }
